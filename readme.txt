- at first install python3 latest version
- then, to install all required packages go to VS code terminal
- type the following command

$ pip install -r requirements.txt


- after that, run app.py file in VS code terminal to start the program using following command


$ python app.py
